<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Allow network access for downloading models from HuggingFace -->
    <key>com.apple.security.network.client</key>
    <true/>

    <!-- Allow microphone access for audio recording -->
    <key>com.apple.security.device.audio-input</key>
    <true/>

    <!-- Allow sending Apple Events for paste simulation -->
    <key>com.apple.security.automation.apple-events</key>
    <true/>

    <!-- Allow loading unsigned libraries (MLX, etc) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>

    <!-- Allow DYLD environment variables -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>

    <!-- Disable library validation for third-party libraries -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
</dict>
</plist>
